import { ArbiterDesk } from "@/components/arbiter-desk";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "Arbiter" };

export default function ArbiterPage() {
  return <ArbiterDesk />;
}
